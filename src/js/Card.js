export default class Card {
  constructor(heading, descripiton, id, date, number) {
    this.heading = heading;
    this.descripiton = descripiton;
    this.id = id;
    this.date = date;
    this.number = number;
  }
}
